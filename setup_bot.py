"""Legacy bootstrap script.

The project files are already created. Do not use this script to regenerate them,
because it would overwrite deployment/database changes.
"""

print("Project is already set up. Edit .env locally, then run: python bot.py")
